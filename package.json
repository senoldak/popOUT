{
  "name": "popout",
  "version": "1.3.0",
  "private": true,
  "description": "popOUT - Smart Popup & Overlay Blocker (Chrome Extension, Manifest V3)",
  "scripts": {
    "check": "node scripts/check.js",
    "build": "node scripts/build_zip.js",
    "lint": "eslint background content popup options scripts"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}